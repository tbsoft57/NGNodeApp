#!/bin/bash
echo Enter commit message
read commitMessage
echo Commit and Push to depot?
read push
if [[ "YyYESyes" == *"$push"* ]] 
then
    git add --all && git commit -m "$commitMessage"
    git push
else
    echo "Canceled"
fi
